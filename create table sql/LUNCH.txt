CREATE TABLE  "LUNCH" 
   (	"L_ITEM_NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"L_PRICE" VARCHAR2(100) NOT NULL ENABLE, 
	"L_QUANTITY" VARCHAR2(100) NOT NULL ENABLE, 
	"IDFOOD" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "LUNCH_PK" PRIMARY KEY ("IDFOOD") ENABLE
   ) ;ALTER TABLE  "LUNCH" ADD CONSTRAINT "LUNCH_FK" FOREIGN KEY ("IDFOOD")
	  REFERENCES  "FOOD_MANAGE" ("IDFOOD") ON DELETE CASCADE ENABLE;