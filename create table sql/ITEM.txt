CREATE TABLE  "ITEM" 
   (	"ITEMNAME" VARCHAR2(100) NOT NULL ENABLE, 
	"PRICE" VARCHAR2(100) NOT NULL ENABLE, 
	"QUANTITY" VARCHAR2(100) NOT NULL ENABLE, 
	"BUYID" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "ITEM_PK" PRIMARY KEY ("BUYID") ENABLE
   ) ;ALTER TABLE  "ITEM" ADD CONSTRAINT "ITEM_FK" FOREIGN KEY ("BUYID")
	  REFERENCES  "FOODBUY" ("BUYID") ON DELETE CASCADE ENABLE;