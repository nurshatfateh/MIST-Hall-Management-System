CREATE TABLE  "EMPLOYEE" 
   (	"EMPLOYEEID" VARCHAR2(100) NOT NULL ENABLE, 
	"DESIGNATION" VARCHAR2(100) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(100) NOT NULL ENABLE, 
	"REPONSIBILITY" VARCHAR2(100) NOT NULL ENABLE, 
	"NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"ADDRESS" VARCHAR2(100) NOT NULL ENABLE, 
	"PHONE" VARCHAR2(100) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(100) NOT NULL ENABLE, 
	"IDFOOD" VARCHAR2(100), 
	"BUYID" VARCHAR2(100), 
	 CONSTRAINT "EMPLOYYE_PK" PRIMARY KEY ("EMPLOYEEID") ENABLE
   ) ;ALTER TABLE  "EMPLOYEE" ADD CONSTRAINT "EMPLOYYE_BUYID_FK" FOREIGN KEY ("BUYID")
	  REFERENCES  "FOODBUY" ("BUYID") ON DELETE CASCADE ENABLE;ALTER TABLE  "EMPLOYEE" ADD CONSTRAINT "EMPLOYYE_IDFOOD_FK" FOREIGN KEY ("IDFOOD")
	  REFERENCES  "FOOD_MANAGE" ("IDFOOD") ON DELETE CASCADE ENABLE;