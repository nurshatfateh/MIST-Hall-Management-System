CREATE TABLE  "FOOD_MANAGE" 
   (	"IDFOOD" VARCHAR2(100) NOT NULL ENABLE, 
	"FOODMANAGE_DATE" DATE NOT NULL ENABLE, 
	"TOTAL_CONSUMEPRICE" VARCHAR2(100) NOT NULL ENABLE, 
	"FOOD_STOCK_ITEM_NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"FOOD_STOCK_UNIT_PRICE" VARCHAR2(100) NOT NULL ENABLE, 
	"FOOD_STOCK_QUANTITY" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "FOOD_MANAGE_PK" PRIMARY KEY ("IDFOOD") ENABLE
   ) ;
