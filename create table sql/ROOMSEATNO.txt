CREATE TABLE  "ROOMSEATNO" 
   (	"SEATNO" VARCHAR2(100) NOT NULL ENABLE, 
	"ROOMNO" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "ROOMSEATNO_PK" PRIMARY KEY ("SEATNO") ENABLE
   ) ;ALTER TABLE  "ROOMSEATNO" ADD CONSTRAINT "ROOMSEATNO_FK" FOREIGN KEY ("ROOMNO")
	  REFERENCES  "ROOM" ("ROOMNO") ENABLE;