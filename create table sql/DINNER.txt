CREATE TABLE  "DINNER" 
   (	"D_ITEM_NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"D_PRICE" VARCHAR2(100) NOT NULL ENABLE, 
	"D_QUANTITY" VARCHAR2(100) NOT NULL ENABLE, 
	"IDFOOD" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "DINNER_PK" PRIMARY KEY ("IDFOOD") ENABLE
   ) ;ALTER TABLE  "DINNER" ADD CONSTRAINT "DINNER_FK" FOREIGN KEY ("IDFOOD")
	  REFERENCES  "FOOD_MANAGE" ("IDFOOD") ON DELETE CASCADE ENABLE;