CREATE TABLE  "MENU_LUNCH" 
   (	"DAY" VARCHAR2(20) NOT NULL ENABLE, 
	"LUNCH" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "MENU_LUNCH_PK" PRIMARY KEY ("DAY") ENABLE
   ) ;ALTER TABLE  "MENU_LUNCH" ADD CONSTRAINT "MENU_LUNCH_FK" FOREIGN KEY ("DAY")
	  REFERENCES  "MENU" ("DAY") ON DELETE CASCADE ENABLE;