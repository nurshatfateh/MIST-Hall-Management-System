CREATE TABLE  "MENU_DINNER" 
   (	"DAY" VARCHAR2(20) NOT NULL ENABLE, 
	"DINNER" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "MENU_DINNER_PK" PRIMARY KEY ("DAY") ENABLE
   ) ;ALTER TABLE  "MENU_DINNER" ADD CONSTRAINT "MENU_DINNER_FK" FOREIGN KEY ("DAY")
	  REFERENCES  "MENU" ("DAY") ENABLE;
