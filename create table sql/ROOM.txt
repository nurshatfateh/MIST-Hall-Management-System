CREATE TABLE  "ROOM" 
   (	"ROOMNO" VARCHAR2(100) NOT NULL ENABLE, 
	"CAPACITY" VARCHAR2(100) NOT NULL ENABLE, 
	"ROOMDATE" DATE NOT NULL ENABLE, 
	"EMPLOYEEID" VARCHAR2(100) NOT NULL ENABLE, 
	"ALLOCATED" VARCHAR2(100) NOT NULL ENABLE, 
	"UNALLOCATED" VARCHAR2(100), 
	 CONSTRAINT "ROOM_PK" PRIMARY KEY ("ROOMNO") ENABLE
   ) ;ALTER TABLE  "ROOM" ADD CONSTRAINT "ROOM_EMPLOYEEID_FK" FOREIGN KEY ("EMPLOYEEID")
	  REFERENCES  "EMPLOYEE" ("EMPLOYEEID") ON DELETE CASCADE ENABLE;
