CREATE TABLE  "FOODBUY" 
   (	"IDFOOD" VARCHAR2(100) NOT NULL ENABLE, 
	"BUYID" VARCHAR2(100) NOT NULL ENABLE, 
	"BUYDATE" DATE NOT NULL ENABLE, 
	 CONSTRAINT "FOODBUY_PK" PRIMARY KEY ("BUYID") ENABLE
   ) ;ALTER TABLE  "FOODBUY" ADD CONSTRAINT "FOODBUY_IDFOOD_FK" FOREIGN KEY ("IDFOOD")
	  REFERENCES  "FOOD_MANAGE" ("IDFOOD") ON DELETE CASCADE ENABLE;