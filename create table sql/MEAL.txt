CREATE TABLE  "MEAL" 
   (	"MEALID" VARCHAR2(100) NOT NULL ENABLE, 
	"MEALDATE" DATE NOT NULL ENABLE, 
	"BREAKFASTSTATUS" VARCHAR2(100) NOT NULL ENABLE, 
	"BREAKFASTFEE" VARCHAR2(100), 
	"LUNCHSTATUS" VARCHAR2(100) NOT NULL ENABLE, 
	"LUNCHFEE" VARCHAR2(100), 
	"DINNERSTATUS" VARCHAR2(100) NOT NULL ENABLE, 
	"DINNERFEE" VARCHAR2(100), 
	"STUDENTID" VARCHAR2(100) NOT NULL ENABLE, 
	 CONSTRAINT "MEAL_PK" PRIMARY KEY ("MEALID") ENABLE
   ) ;ALTER TABLE  "MEAL" ADD CONSTRAINT "MEAL_FK" FOREIGN KEY ("STUDENTID")
	  REFERENCES  "ADMISSION_APPLICANTS" ("STUDENTID") ENABLE;